<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthhub_cm">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- LAUNCHER — first screen users see -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Buyer home screen -->
        <activity
            android:name=".BuyerHomeActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Coursemate's activities — leave these for them -->
        <activity android:name=".ui.ResultActivity"/>
        <activity android:name=".ui.SellerActivity"/>
        <activity android:name=".ui.BuyerActivity"/>

    </application>

</manifest>